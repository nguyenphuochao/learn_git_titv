- repository(repo): kho lưu trữ
- commit:  một đơn vị làm việc
- branch: nhánh
- main/master: tên của repo chính(main repo)
- merge/rebase: kết hợp 2 nhánh
- develop: tên của nhánh lập trình viên
- git --help: trợ giúp,hướng dẫn
- git --version: hiển thị thông tin phiên bản git
- git status: hiển thị trạng thái kho lưu trữ
- git log: hiển thị lịch sử các commit
==== Câu lệnh cấu hình dự án git mới ===
- git init [repo name] : tạo 1 kho lưu trữ
- git clone [repo name] [clone name]: để tạo 1 bản sao dc liên kết vs repo
- git config -l: xem cấu hình hiện tại
- git config -l [--scope][option_name][value]:
   scope: -- system -> tất cả người dùng
          -- global -> liên quan đế repo(s)
          -- local  -> liên quan đến 1 repo
== thực hành ==
 1. Chọn thư mục
 2. git bash
 3. git init Ex1
 4. cd Ex1
 5. Kiểm tra thông tin bên trong 


